#include "prog.h"

int checkMov(char * row,int i)
{
    char param1[MAX], param2[MAX];//***
    getNextWord(row,i,param1)
    if(!isvalid())//  נשלח פרמטרים מתאימים, בדיקה האם שורת הפקודת תקינה וניתנת לביצוע, אם לא אז בבדיקה עצמה יודפס השגיאה
        return 0;
    //copy param1 into param2
    return 1;
}
